import { SchemeConfigModel } from '../schemeConfiguration/schemeConfigModel';

/**
 * Every global constraint configuration model must confirm to this.
 */
export interface GlobalConstraintConfigModel extends SchemeConfigModel {
}
